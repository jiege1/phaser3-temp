
import Phaser from 'phaser';
import Base from 'game/common/models/base';

export default class Container extends Base {

  container = new Phaser.GameObjects.Container();

  constructor(props) {
    super(props);
  }

  render() {
    // this.
  }

}
